# build output
/bin/
/big2-tui

# stray binaries from `go build ./cmd/...`
/server
/preview
/sshprobe

# host key
/.ssh/

# logs, coverage, generated preview dump
*.log
*.out
/ui-preview.txt

# persisted preferences (written beside the binary)
big2-tui.prefs.json*

# macOS
.DS_Store
